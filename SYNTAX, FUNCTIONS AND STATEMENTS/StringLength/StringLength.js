function stringLength(a,b,c){
    let sumOfLength=a.length+b.length+c.length;
    let averageSum=Math.floor(sumOfLength/3);
    console.log(sumOfLength);
    console.log(averageSum);
}
stringLength('sads', 'iasdm', 'csde')